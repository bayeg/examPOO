{% extends "::base.html.twig" %}

{% block title %}eventDetail{% endblock %}

{% block main %}
{#    <img src="{{asset(event.image.url)}}"/>#}
    <p>{{"Nom : "}}{{event.nom}}</p>
    <p>{{"Description : "}}{{event.description}}</p>
    <p>{{"Début : "}}{{event.debut|date("d/m/Y")}}</p>
    <p>{{"Fin : "}}{{event.fin|date("d/m/Y")}}</p>
{#    <p>{{"Catégorie : "}}{{event.categorie.nom}}</p>#}
    <p>{{"Participants : "}}
    <ul>
        {% for e in event.user %}
            <li>{{e.username}}</li>
            {%endfor%}
    </ul>
</p>

{% endblock %}

